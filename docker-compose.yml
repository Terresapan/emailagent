services:
  # Email processor (existing - runs at 7am via cron)
  emailagent:
    build: ./backend
    container_name: emailagent
    restart: unless-stopped
    environment:
      - TZ=America/Los_Angeles
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/contentagent
    volumes:
      # Mount credentials and tokens
      - ./backend/credentials.json:/app/credentials.json:ro
      - ./backend/token.json:/app/token.json
      - ./backend/.env:/app/.env:ro
      # Mount logs for persistence
      - ./backend/logs:/var/log/emailagent
      # Mount code directories for hot-reloading
      - ./backend/main.py:/app/main.py
      - ./backend/config:/app/config
      - ./backend/db:/app/db
      - ./backend/processor:/app/processor
      - ./backend/sources:/app/sources
      - ./backend/utils:/app/utils
    env_file:
      - ./backend/.env
    depends_on:
      db:
        condition: service_healthy

  # FastAPI backend
  api:
    build:
      context: ./backend
      dockerfile: api/Dockerfile
    container_name: contentagent-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - TZ=America/Los_Angeles
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/contentagent
    env_file:
      - ./backend/.env
    volumes:
      # Mount API code for hot-reloading
      - ./backend/api:/app
      # Mount pyproject.toml for uv to find the venv
      - ./backend/pyproject.toml:/app/pyproject.toml:ro
      # Preserve venv from image
      - /app/.venv
      # Mount shared packages
      - ./backend/db:/app/db
      - ./backend/sources:/app/sources
      - ./backend/processor:/app/processor
      - ./backend/config:/app/config
      - ./backend/utils:/app/utils
      # Mount Gmail credentials for email delivery
      - ./backend/credentials.json:/app/credentials.json:ro
      - ./backend/token.json:/app/token.json
      # Mount logs for usage tracking
      - ./backend/logs:/app/logs
      # Mount docker socket to enable triggering emailagent
      - /var/run/docker.sock:/var/run/docker.sock
    depends_on:
      db:
        condition: service_healthy

  # Next.js dashboard (with hot-reload for development)
  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile.dev
    container_name: contentagent-dashboard
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      - WATCHPACK_POLLING=true
    volumes:
      # Mount source code for hot-reloading
      - ./dashboard/src:/app/src
      - ./dashboard/public:/app/public
      # Mount config files for hot-reloading (no rebuild needed)
      - ./dashboard/tailwind.config.js:/app/tailwind.config.js
      - ./dashboard/postcss.config.mjs:/app/postcss.config.mjs
    depends_on:
      - api

  # PostgreSQL database
  db:
    image: postgres:15
    container_name: contentagent-db
    restart: unless-stopped
    environment:
      - POSTGRES_DB=contentagent
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d contentagent"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
